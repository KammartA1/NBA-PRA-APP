# NBA Alpha Engine — Streamlit Secrets
# Add your Anthropic API key here so ALL logged-in users can use Claude AI features.
# This key is never exposed to users in the UI.

ANTHROPIC_API_KEY = ""

# Optional: The Odds API key
# ODDS_API_KEY = ""
